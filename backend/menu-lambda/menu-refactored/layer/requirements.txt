# layer/requirements.txt
# ========================
# Dependencies for the shared Lambda Layer.
# boto3 is provided by the Lambda runtime — NOT listed here.
# Only add packages that are NOT part of the standard Lambda runtime.

# (Currently no external dependencies — all shared code is pure Python.)
# Add third-party packages here if needed, e.g.:
# aws-xray-sdk>=2.12.0
