# requirements.txt (Lambda function level)
# ==========================================
# boto3 is provided by the Lambda runtime — listed for LOCAL TESTING only.
# The shared layer has its own layer/requirements.txt.
boto3>=1.34.0
