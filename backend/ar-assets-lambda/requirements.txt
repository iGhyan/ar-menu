# ar-assets-lambda/requirements.txt
# =====================================
# boto3 is provided by the Lambda runtime — listed for local dev only.
# Shared layer has its own layer/requirements.txt.
boto3>=1.34.0

# Test / CI only
pytest>=8.0
pytest-cov>=5.0
